{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "python src/manage.py migrate && python src/create_admin.py && gunicorn --chdir src config.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
